rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This allows anyone to read/write for 30 days.
    // This is for TESTING ONLY.
    match /{document=**} {
      allow read, write: if request.time < timestamp.now() + duration("30d");
    }
  }
}